<% flash.each do |type, message| %>
  <div class="alert fade in <%= twitterized_type(type) %>" data-alert="alert">
    <a class="close" href="#">×</a>
    <p>
      <%= message %>
    </p>
  </div>
<% end %>