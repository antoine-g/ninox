<h1>All documents</h1>

<%= paginate @documents %>

<table class="documents">
  <tr>
    <th>Title</th><th>Description</th><th>Course</th>
  </tr>
  <%= render @documents %>
</table>

<%= paginate @documents %>